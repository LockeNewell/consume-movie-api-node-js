let substr = 'spiderman';
let url = `https://jsonmock.hackerrank.com/api/movies/search/?Title=${substr}`;
let task = ['consume api at the url for a given substring',
            'create a list of titles',
            'sort the list of titles'
        ];
let note = 'the the object returned has a page and total_page var';
let sampleOutput = 'is in movie.stream';